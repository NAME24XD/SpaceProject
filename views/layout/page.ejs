<!DOCTYPE html>
<html>
<head>
<title><%= title %></title>
<link rel='stylesheet' href='/stylesheets/style.css' />
<link rel="icon" href="https://icons.veryicon.com/png/o/education-technology/educational-icon/space-3.png">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" type="text/css">
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.min.js"></script>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-light navbar-light">
        <div class="container-fluid">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/">Главная</a>
                </li>
                <% menu.forEach(space => { %>
                    <li class="nav-item">
                      <a class="nav-link" href="/space/<%= space.nick %>"><%= space.title %></a>
                    </li>
                  <% }) %>
            </ul>

            <ul class="navbar-nav navbar-right">
                <!-- <li>
                    <a class="nav-link" href="/form">Войти</a>
                </li> -->
            <div class="dropdown">
                <button class="dropbtn">Войти</button>
                <div class="dropdown-content">
                    <form action="#" method="get">
                        <input type="text" placeholder="name" required>
                        <input type="password" placeholder="password" required>
                        <input type="email" placeholder="e-mail" required>
                        <input type="submit" value="Войти">
                        <input type="reset" value="Очистить">
                    </form>
                </div>
            </div>
            </ul>
        </div>
    </nav>
    
    <%- body %>
</body>

    <!-- <script>
        if (document.cookie.split(';').filter((item) => item.trim().startsWith('_ga=')).length) {
    console.log('Куки с именем "_ga" уже существует');
    } else {
    // Создаем куки с именем '_ga'
    document.cookie = "_ga=GA1.2.1929734891.1627674222; max-age=31536000; SameSite=None; Secure";
    console.log('Куки с именем "_ga" успешно добавлены');
    }

    if (document.cookie.split(';').filter((item) => item.trim().startsWith('greeting=')).length) {
    const greetingCookie = document.cookie
    .split('; ')
    .find((row) => row.startsWith('greeting='))
    .split('=')[1]; // Получаем значение куки 'greeting'
    console.log('Значение куки "greeting":', greetingCookie);
    } else {
    console.log('Куки с именем "greeting" не найдены');
    }
    </script> -->

</html>